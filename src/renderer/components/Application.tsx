import { hot } from 'react-hot-loader/root';
import * as React from 'react';

const Application = () => {
    return (
        <div>
            <div>Hello World from Electron!</div>
            <small>Customized by TheUnderScorer.</small>
        </div>
    );
};

export default hot(Application);
